# 漫画編集Canvas MVP

更新日: 2026-07-13

## 現在の判定

MANGAI Desktopで、矩形コマ、素材画像、吹き出し、縦書き・横書きテキスト、レイヤー、テンプレート、Undo/Redo、PDF・画像ZIP書き出しの基本経路を利用できます。

完了条件33項目の広義な機能・ビルド判定は満たしています。ただし、元指示書の詳細要件と4つの手動シナリオには未確認・未実装が残るため、漫画編集Canvas MVP全体の判定は「一部完了」です。個別判定は [`MANGA_EDITOR_IMPLEMENTATION_STATUS.md`](MANGA_EDITOR_IMPLEMENTATION_STATUS.md) を参照してください。

## 対応機能

### コマと画像

- 矩形コマの追加、選択、移動、リサイズ、回転、複製、削除
- 位置、寸法、名称、境界線、背景色の編集
- 素材の割り当て、差し替え、解除
- `cover`、`contain`、`manual`の画像配置
- 画像倍率、オフセット、回転、不透明度
- コマ領域での画像クリッピング
- ページ端、中央、他オブジェクトへのスナップとガイド
- Shift選択による複数オブジェクトの一括移動、複製、削除

### 吹き出しとテキスト

- 楕円、角丸、ナレーション枠
- 8方向のしっぽと位置調整
- 吹き出し作成時の子テキスト自動生成
- 自由テキストと吹き出し親子設定
- 吹き出し移動・リサイズ時の子テキスト追従と領域再計算
- 横書き、縦書き、文字サイズ、色、揃え
- grapheme単位の文字分割と縦書き用句読点・括弧表示
- 600msデバウンス保存と文字あふれ警告

### レイヤーとテンプレート

- コマ、吹き出し、テキストの統合一覧
- 前後移動、表示、ロック、選択
- 1コマ全面、縦2分割、横2分割、上1＋下2、4コマ、6コマ
- 既存コマの置換・追加と、テンプレート適用の単一Undo

### 書き出し

- Page原寸の合成PNG
- JPG、PNG、WebP素材の混在
- Episode・Page正式順の連番PNG ZIP
- DPIから物理サイズを算出するPDF
- 非表示レイヤーの除外
- ページ単位の進捗、キャンセル、失敗理由
- Canvasを持たない旧Page画像の全面背景互換

## 画面と操作

- 左: Project、Episode、Page、素材
- 中央: ページCanvas、ズーム、Canvas追加操作
- Canvas右: 統合レイヤーと選択オブジェクトのプロパティ
- アプリ右: Project、Page、素材の既存情報
- 上部: Undo/Redo、操作履歴、インポート、AI、書き出し、設定

基本操作:

1. EpisodeへPageを追加します。
2. テンプレートを選ぶか「＋ コマ」で矩形コマを追加します。
3. 左の素材を選び、コマ選択後に「素材を配置」を実行します。
4. 「＋ 吹き出し」「＋ テキスト」で要素を追加します。
5. レイヤーまたはCanvasで要素を選び、プロパティを編集します。
6. 上部の「書き出し」でPDFと画像ZIPを生成します。

## ショートカット

- `Ctrl+Z`: Undo
- `Ctrl+Y` / `Ctrl+Shift+Z`: Redo
- `Delete` / `Backspace`: 選択を削除
- `Ctrl+D`: 選択を複製
- 矢印キー: 1px移動
- `Shift` + 矢印キー: 10px移動
- Shiftクリック: 複数選択の追加・解除

入力欄へフォーカス中と、ロック済み要素を含む選択ではCanvasショートカットを抑止します。

## データ構造と保存

- 正本はSQLiteの`pages`、`panels`、`balloons`、`text_objects`です。
- Canvas内部JSONは永続データの正本にしません。
- 座標と寸法は表示倍率に依存しないPage原寸ピクセルです。
- Canvas IPC入力は`@mangai/canvas-core`のZod schemaで検証します。
- DBマイグレーション前に旧SQLiteをバックアップし、`schema_migrations`で適用済み版を管理します。
- drag/transformは操作終了時に保存し、文字本文はデバウンス保存します。
- rendererはSQLiteやNode.js APIを直接利用せず、contextBridge経由でmain processへ依頼します。

## Undo / Redo

Canvasスナップショットv2はPage設定、Panel、Balloon、Text、z-index、表示、ロック、親子IDを保存します。画像ファイル本体とBase64は履歴へ含めません。旧スナップショットは後方互換で読み込みます。

複数移動・複製・削除、テンプレート適用、吹き出しと子テキストの削除は一括保存し、1回のUndoとして扱います。

## 制限事項・既知の問題

- コマは既定サイズ追加とテンプレートに対応していますが、ページ上をドラッグする作成モードは未実装です。
- 素材選択後の割り当ては可能ですが、素材一覧からコマへの直接ドラッグ＆ドロップは未実装です。
- 画像調整はプロパティ入力で行い、ダブルクリック、Canvas内直接操作、Escape終了を備えた画像編集専用モードは未実装です。
- グリッド表示、グリッドスナップ、スナップ無効化UIは未実装です。
- 親子テキストは移動・リサイズへ追従しますが、SQLiteにはPage座標として保存します。親基準の相対座標を正本にする移行は未実施です。
- レイヤーのボタン並び替えはありますが、ドラッグ＆ドロップ並び替えは未実装です。
- 高度な禁則、ルビ、縦中横、商用フォント同梱は対象外です。
- Canvas領域はPage原寸×ズームでスクロールします。狭いウィンドウではレイヤーパネルを見るため横スクロールが必要です。
- rendererのVite chunkが500kBを超える警告が残っています。
- 30オブジェクトPageの実操作性能と、クリーンWindowsへのインストールは未確認です。

## 手動受け入れ記録

2026-07-13のパッケージ版確認:

- 新規Project、Episode、Page: 成功
- 4コマテンプレート: 成功。レイヤー4件を確認
- 吹き出しと縦書きテキスト: 成功
- Undo/Redo: 成功
- アプリ終了後のProject、4コマ、吹き出し、テキスト復元: 成功
- シナリオA: 一部完了。4素材配置とUIからのPDF/ZIP書き出しは未実施
- シナリオB: 一部完了。Undo/Redoと再起動復元は確認、複数回の全操作列は未実施
- シナリオC: 自動テスト成功、旧Projectを使った手動確認は未実施
- シナリオD: 自動テスト成功、JPG/PNG/WebPを使った手動確認は未実施

パッケージ確認中に、sandboxed preloadがES Modulesとして生成されるため黒画面になる問題を検出しました。preloadをCommonJSとして生成し、修正版NSISと`win-unpacked`で正常起動を確認しています。
