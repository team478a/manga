# Include inside the HTTPS server block that proxies MANGAI Hub.
# 20 MiB Cloud Asset + at most 1 MiB multipart envelope.
client_max_body_size 21m;
client_body_buffer_size 256k;
client_body_timeout 30s;
