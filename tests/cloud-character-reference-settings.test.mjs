import assert from "node:assert/strict";import{readFile}from"node:fs/promises";import test from"node:test";import{characterReferenceBindingInputSchema,characterStateAssignmentInputSchema}from"../src/lib/cloud-character-reference-settings.ts";
const id=n=>`${n}0000000-0000-4000-8000-000000000001`;
test("人物参照roleは表情名と承認状態を検証する",()=>{const base={projectId:id(1),characterProfileId:id(2),characterVersionId:id(3),assetId:id(4),role:"face",expressionKey:"",priority:50,reviewStatus:"approved"};assert.equal(characterReferenceBindingInputSchema.parse(base).role,"face");assert.equal(characterReferenceBindingInputSchema.safeParse({...base,role:"expression"}).success,false);});
test("衣装状態範囲は終了ページ逆転を拒否する",()=>{const base={projectId:id(1),characterProfileId:id(2),characterVersionId:id(3),startPage:4,endPage:8,sceneKey:"",label:"制服",costumeOverride:"紺の制服",stateNote:"",priority:0};assert.equal(characterStateAssignmentInputSchema.parse(base).endPage,8);assert.equal(characterStateAssignmentInputSchema.safeParse({...base,endPage:3}).success,false);});
test("P1-C画面はversion role 承認 方針 衣装範囲を操作できる",async()=>{const page=await readFile("src/app/creator/[projectId]/references/page.tsx","utf8"),actions=await readFile("src/app/creator/[projectId]/references/actions.ts","utf8");for(const pattern of[/人物versionへ参照roleを設定/,/確認状態/,/生成準備方針/,/衣装・状態の適用範囲/])assert.match(page,pattern);for(const pattern of[/saveCharacterReferenceBindingAction/,/saveCharacterStateAssignmentAction/,/saveGenerationReadinessPolicyAction/])assert.match(actions,pattern);});
